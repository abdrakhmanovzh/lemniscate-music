import { Button } from '@/shared/components/ui/button'

export function HeroLoginButton() {
  return (
    <Button className="w-fit rounded-xl text-lg" size={'lg'}>
      start now
    </Button>
  )
}
